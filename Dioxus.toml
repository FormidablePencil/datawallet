[application]
name = "datawallet"
default_platform = "fullstack"
out_dir = "public"

# For pure web mode, use: dx serve --platform web --features web
# Note: Pure web mode uses browser localStorage instead of file system

[web.app]
title = "(dynamic) DataWallet"
base_path = "datawallet"   

[web.watcher]
watch_path = ["src", "public"]

[web.resource]
style = []
script = []

[web.resource.dev]
style = []
script = []

#[server]
#cargo = true
